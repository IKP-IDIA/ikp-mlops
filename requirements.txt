# --- Base dependencies ---
numpy>=1.23.5
scipy>=1.10.1
pandas==2.0.3
scikit-learn==1.3.2

# --- Core MLOps & Serving ---
kserve==0.12.0
mlflow==2.19.0
evidently

# --- Support Libraries (ปล่อยให้ pip เลือกเวอร์ชันที่เข้ากับ KServe เอง) ---
fastapi
uvicorn
pydantic
boto3
joblib
python-dotenv
wheel
setuptools