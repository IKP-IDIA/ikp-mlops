# --- Base & MLOps ---
numpy>=1.23.5
scipy>=1.10.1
pandas==2.0.3
scikit-learn==1.3.2
kserve==0.12.0
mlflow==2.19.0
evidently

# --- Utilities (ตัวแสบที่หายไป) ---
python-box
ensure
python-dotenv
joblib
boto3
wheel
setuptools

# --- API Framework (ปล่อยให้ pip เลือกเวอร์ชันที่เข้ากับ KServe) ---
fastapi
uvicorn
pydantic